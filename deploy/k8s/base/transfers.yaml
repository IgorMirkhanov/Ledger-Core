apiVersion: v1
kind: ServiceAccount
metadata:
  name: transfers
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfers
  labels: {app.kubernetes.io/name: transfers}
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels: {app.kubernetes.io/name: transfers}
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxUnavailable: 0, maxSurge: 1}
  template:
    metadata:
      labels: {app.kubernetes.io/name: transfers, app.kubernetes.io/part-of: ledger-core}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: /metrics
    spec:
      serviceAccountName: transfers
      automountServiceAccountToken: false
      # > SHUTDOWN_TIMEOUT (15s) + preStop (5s): the app drains before SIGKILL.
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: {app.kubernetes.io/name: transfers}
      containers:
        - name: transfers
          image: ghcr.io/igormirkhanov/ledger-core-transfers:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef: {name: ledger-config}
          env:
            - name: SERVICE_NAME
              value: transfers
            - name: POSTGRES_DSN
              valueFrom:
                secretKeyRef: {name: ledger-secrets, key: TRANSFERS_POSTGRES_DSN}
            - name: RECOVERY_INTERVAL
              value: "1s"
          ports:
            - name: grpc
              containerPort: 9090
            - name: admin
              containerPort: 8081
          startupProbe:
            httpGet: {path: /healthz, port: admin}
            periodSeconds: 2
            failureThreshold: 30
          livenessProbe:
            httpGet: {path: /healthz, port: admin}
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet: {path: /readyz, port: admin}
            periodSeconds: 5
            failureThreshold: 2
          lifecycle:
            # Keep serving while endpoints propagate removal of this pod (Kubernetes >= 1.30).
            preStop:
              sleep: {seconds: 5}
          resources:
            requests: {cpu: 250m, memory: 256Mi}
            limits: {cpu: 1, memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: transfers
spec:
  minAvailable: 1
  selector:
    matchLabels: {app.kubernetes.io/name: transfers}
---
# Headless: DNS returns every pod IP so gRPC clients balance round_robin across replicas.
apiVersion: v1
kind: Service
metadata:
  name: transfers-grpc
spec:
  clusterIP: None
  selector: {app.kubernetes.io/name: transfers}
  ports:
    - {name: grpc, port: 9090, targetPort: grpc, appProtocol: grpc}
---
apiVersion: v1
kind: Service
metadata:
  name: transfers-admin
  labels: {app.kubernetes.io/name: transfers}
spec:
  selector: {app.kubernetes.io/name: transfers}
  ports:
    - {name: admin, port: 8081, targetPort: admin}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: transfers
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: transfers}
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: {type: Utilization, averageUtilization: 70}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
