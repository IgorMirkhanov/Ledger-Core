apiVersion: v1
kind: Namespace
metadata:
  name: ledger
  labels:
    # Enforce the restricted Pod Security Standard for every workload in the namespace.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
