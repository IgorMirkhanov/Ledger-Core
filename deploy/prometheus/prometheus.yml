global:
  scrape_interval: 5s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: ledger
    static_configs:
      - targets:
          - gateway:8081
          - accounts:8081
          - transfers:8081
          - notifications:8081
